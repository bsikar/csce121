#include <iostream>
#include <string>
using namespace std;

int main() {
  string userName;
  string cityName;
  string action = "lives in";

  cin >> userName;
  cin >> cityName;

  cout << userName << " lives in " << cityName << "." << endl;

  return 0;
}
