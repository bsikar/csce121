#include "mystery_formula.h"
#include <iostream>

using std::cout, std::endl;

int main() {
  bool w = 0;
  bool x = 1;
  bool y = 1;
  bool z = 1;
  // w=0, x=1, y=1, z=1
  //  w=1, x=1, y=0, z=0
  f(w, x, y, z);
}
